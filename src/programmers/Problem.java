package programmers;

public class Problem {

    public static void main(String[] args) {
        Problem p = new Problem();

        int t = 1;
        //t = Util.getTestCaseCount();

        for (int i = 0; i < t; i++) {
            /*
             * Type var = getTypeArr[]
             * Type ...
             * ...
             * sout(p.solution());
             */
        }
    }
}
